# Docker Compose — Project Sentinel
#
# Runs the full observability stack: Prometheus (scraping + alerting),
# AlertManager (alert routing), and Grafana (dashboards).
# lab-rat, sentinel-agent, and sentinel-dashboard all run as containers
# on the shared sentinel-net Docker network.

services:

  # ---------------------------------------------------------------------------
  # Prometheus
  # Scrapes lab-rat metrics, evaluates alert rules, and forwards firing alerts
  # to AlertManager. --web.enable-lifecycle allows hot-reloading the config via
  # POST /-/reload without restarting the container.
  # ---------------------------------------------------------------------------
  prometheus:
    # Pinned for reproducibility — see VERSIONS.md before bumping.
    image: prom/prometheus:v3.0.1
    container_name: sentinel-prometheus
    ports:
      - "9090:9090"
    volumes:
      # Main Prometheus config
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      # Alert rules evaluated every scrape cycle
      - ./alert-rules.yml:/etc/prometheus/alert-rules.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      # Enables POST /-/reload for config hot-reload without container restart
      - '--web.enable-lifecycle'
    depends_on:
      - alertmanager
    networks:
      - sentinel-net

  # ---------------------------------------------------------------------------
  # AlertManager
  # Receives alerts from Prometheus, groups them, applies silence/inhibition
  # rules, and routes them to the configured receiver (sentinel-agent webhook).
  # ---------------------------------------------------------------------------
  alertmanager:
    # Pinned for reproducibility — see VERSIONS.md before bumping.
    image: prom/alertmanager:v0.27.0
    container_name: sentinel-alertmanager
    ports:
      # AlertManager UI and Prometheus-to-AlertManager API
      - "9093:9093"
    volumes:
      # Routing and receiver configuration
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
    networks:
      - sentinel-net

  # ---------------------------------------------------------------------------
  # Grafana
  # Visualization layer. Datasource and dashboard setup is done manually via
  # the UI or via provisioning files (not yet configured).
  # Grafana automatically connects to datasources configured in the UI.
  # ---------------------------------------------------------------------------
  grafana:
    # Pinned for reproducibility — see VERSIONS.md before bumping.
    image: grafana/grafana:11.4.0
    container_name: sentinel-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
    volumes:
      # Auto-provision the Prometheus datasource and the Sentinel self-observability
      # dashboard so a fresh `docker compose up` lands on a working Grafana with
      # zero manual setup. Edits to either file are picked up within 30s via the
      # provider's updateIntervalSeconds.
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/etc/grafana/dashboards
    depends_on:
      - prometheus
    networks:
      - sentinel-net

  # ---------------------------------------------------------------------------
  # Postgres
  # Persistent database for sentinel-agent investigations.
  # Always active in Docker — datasource config lives in application-docker.properties.
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: sentinel-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=sentinel
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - sentinel-postgres-data:/var/lib/postgresql/data
    networks:
      - sentinel-net

  lab-rat:
    build:
      context: .
      dockerfile: lab-rat/Dockerfile
    container_name: sentinel-lab-rat
    ports:
      - "8080:8080"
    volumes:
      - lab-rat-logs:/logs
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - sentinel-net

  sentinel-agent:
    build:
      context: .
      dockerfile: sentinel-agent/Dockerfile
    container_name: sentinel-agent
    ports:
      - "8081:8081"
    volumes:
      - lab-rat-logs:/logs
      # Phase 2 patch applier: bind-mount lab-rat source so APPROVED patches
      # can be written back to disk. Backups go to .sentinel-backups inside
      # this same mount. Read-write — this IS the file-write surface.
      - ./lab-rat/src:/lab-rat-src
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    depends_on:
      - postgres
      - lab-rat
    networks:
      - sentinel-net
    # Uncomment on Linux to enable JVM profiling across containers:
    # pid: "host"

  sentinel-dashboard:
    build:
      context: sentinel-dashboard
      dockerfile: Dockerfile
    container_name: sentinel-dashboard
    ports:
      - "5173:80"
    depends_on:
      - sentinel-agent
    networks:
      - sentinel-net

volumes:
  sentinel-postgres-data:
  lab-rat-logs:

networks:
  sentinel-net:
    driver: bridge
